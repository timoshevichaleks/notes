# AI Notes — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Построить учебный full-stack проект — сервис заметок, где фоновый воркер генерирует AI-summary через Claude API; покрывает auth, БД-модели, миграции, фоновые задачи, интеграцию, CI/CD и документацию.

**Architecture:** NestJS API кладёт задачи в очередь BullMQ (Redis); отдельный воркер-процесс обрабатывает их через Anthropic SDK и пишет результат в PostgreSQL (Prisma). Angular SPA общается с API по JWT. Весь стек поднимается через Docker Compose.

**Tech Stack:** NestJS, Prisma, PostgreSQL, BullMQ + Redis, @anthropic-ai/sdk, Passport-JWT, class-validator, pino; Angular (standalone + signals); Jest + supertest; Docker Compose; GitHub Actions.

**Структура каталогов проекта (создаётся в `c:\TransPerfect\test`):**
```
test/
├── api/                  # NestJS backend
│   ├── prisma/
│   ├── src/
│   └── test/             # e2e
├── web/                  # Angular frontend
├── docker-compose.yml
├── .github/workflows/ci.yml
├── README.md
└── RUNBOOK.md
```

**Важно про окружение:** оболочка — PowerShell. Команды `npm` в шагах даны в кросс-платформенном виде; пути — Windows-style там, где это локальные команды.

> **⛔ БЕЗ КОММИТОВ И БЕЗ git.** Это учебный пример. Шаги «Commit» в этом плане **пропускаются** — никаких `git init`/`git add`/`git commit` не выполняем. Воспринимай каждый шаг «Commit» как **чекпоинт**: «остановись, проверь, что текущий кусок работает, и иди дальше».
>
> **🧱 Полная изоляция.** Весь проект создаётся **только** внутри `c:\TransPerfect\test\` (папки `api/`, `web/`, файлы compose/CI/доки). Ничего за пределами этой папки не трогаем — другие проекты (`ReefReview`, `ReefSDK` и т.п.) лежат в соседних каталогах `c:\TransPerfect\` и никак не затрагиваются. Docker-контейнеры именуем с префиксом `ainotes-`, чтобы не конфликтовать с чужими.

---

## Phase 0 — Scaffolding (каркас backend)

### Task 1: Инициализация NestJS-проекта

**Files:**
- Create: `api/` (через CLI)

- [ ] **Step 1: Создать NestJS-проект**

Run:
```
npx -y @nestjs/cli new api --package-manager npm --skip-git
```
Expected: создаётся папка `api/` со стандартной структурой, ставятся зависимости.

- [ ] **Step 2: Проверить, что запускается**

Run (из `api/`):
```
npm run start
```
Expected: лог `Nest application successfully started`, `GET http://localhost:3000` отдаёт `Hello World!`. Останови (Ctrl+C).

- [ ] **Step 3: Поставить базовые зависимости**

Run (из `api/`):
```
npm install @prisma/client class-validator class-transformer @nestjs/config
npm install -D prisma
```
Expected: пакеты добавлены в `package.json`.

- [ ] **Step 4: Включить глобальную валидацию**

Modify: `api/src/main.ts` — добавить `ValidationPipe`:
```typescript
import { ValidationPipe } from '@nestjs/common';
import { NestFactory } from '@nestjs/core';
import { AppModule } from './app.module';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);
  app.useGlobalPipes(
    new ValidationPipe({ whitelist: true, transform: true }),
  );
  app.enableCors();
  await app.listen(process.env.PORT ?? 3000);
}
bootstrap();
```

- [ ] **Step 5: Чекпоинт (без git)**

Убедись, что `api/` создан и `npm run start` работает. Никаких коммитов — просто двигаемся дальше.

---

### Task 2: Prisma + PostgreSQL + первая миграция

**Files:**
- Create: `api/prisma/schema.prisma`
- Create: `api/src/prisma/prisma.service.ts`
- Create: `api/src/prisma/prisma.module.ts`
- Create: `api/.env`

- [ ] **Step 1: Инициализировать Prisma**

Run (из `api/`):
```
npx prisma init --datasource-provider postgresql
```
Expected: создаётся `prisma/schema.prisma` и `.env` с `DATABASE_URL`.

- [ ] **Step 2: Прописать локальный DATABASE_URL**

Modify: `api/.env`:
```
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ainotes?schema=public"
REDIS_HOST="localhost"
REDIS_PORT=6379
JWT_SECRET="dev-secret-change-me"
# ANTHROPIC_API_KEY оставь пустым — заработает мок
ANTHROPIC_API_KEY=""
```

- [ ] **Step 3: Описать модели**

Modify: `api/prisma/schema.prisma` (блок `model`):
```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum NoteStatus {
  PENDING
  PROCESSING
  DONE
  FAILED
}

model User {
  id           String   @id @default(uuid())
  email        String   @unique
  passwordHash String
  notes        Note[]
  createdAt    DateTime @default(now())
}

model Note {
  id        String     @id @default(uuid())
  userId    String
  user      User       @relation(fields: [userId], references: [id], onDelete: Cascade)
  title     String
  content   String
  summary   String?
  tags      String[]
  status    NoteStatus @default(PENDING)
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt
}
```

- [ ] **Step 4: Поднять локальный Postgres (Docker) для миграции**

Run:
```
docker run --name ainotes-pg -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=ainotes -p 5432:5432 -d postgres:16
```
Expected: контейнер запущен (`docker ps` показывает `ainotes-pg`).

- [ ] **Step 5: Создать первую миграцию**

Run (из `api/`):
```
npx prisma migrate dev --name init
```
Expected: создаётся `prisma/migrations/<ts>_init/`, в БД появляются таблицы, генерируется Prisma Client.

- [ ] **Step 6: Создать PrismaService**

Create: `api/src/prisma/prisma.service.ts`:
```typescript
import { Injectable, OnModuleInit } from '@nestjs/common';
import { PrismaClient } from '@prisma/client';

@Injectable()
export class PrismaService extends PrismaClient implements OnModuleInit {
  async onModuleInit() {
    await this.$connect();
  }
}
```

Create: `api/src/prisma/prisma.module.ts`:
```typescript
import { Global, Module } from '@nestjs/common';
import { PrismaService } from './prisma.service';

@Global()
@Module({
  providers: [PrismaService],
  exports: [PrismaService],
})
export class PrismaModule {}
```

- [ ] **Step 7: Подключить PrismaModule и ConfigModule**

Modify: `api/src/app.module.ts`:
```typescript
import { Module } from '@nestjs/common';
import { ConfigModule } from '@nestjs/config';
import { PrismaModule } from './prisma/prisma.module';

@Module({
  imports: [
    ConfigModule.forRoot({ isGlobal: true }),
    PrismaModule,
  ],
})
export class AppModule {}
```
(Удали дефолтные `AppController`/`AppService` из imports/providers, если они мешают; файлы можно оставить.)

- [ ] **Step 8: Коммит**

Run (из `test/`):
```
git add -A
git commit -m "feat: prisma schema, models and initial migration"
```

---

## Phase 1 — Auth

### Task 3: Регистрация и логин (JWT)

**Files:**
- Create: `api/src/auth/auth.module.ts`
- Create: `api/src/auth/auth.service.ts`
- Create: `api/src/auth/auth.controller.ts`
- Create: `api/src/auth/dto/auth.dto.ts`
- Create: `api/src/auth/jwt.strategy.ts`
- Create: `api/src/auth/jwt-auth.guard.ts`
- Test: `api/src/auth/auth.service.spec.ts`

- [ ] **Step 1: Поставить зависимости auth**

Run (из `api/`):
```
npm install @nestjs/jwt @nestjs/passport passport passport-jwt bcrypt
npm install -D @types/passport-jwt @types/bcrypt
```

- [ ] **Step 2: Написать падающий тест AuthService**

Create: `api/src/auth/auth.service.spec.ts`:
```typescript
import { Test } from '@nestjs/testing';
import { JwtService } from '@nestjs/jwt';
import { ConflictException } from '@nestjs/common';
import { AuthService } from './auth.service';
import { PrismaService } from '../prisma/prisma.service';

describe('AuthService', () => {
  let service: AuthService;
  let prisma: { user: { findUnique: jest.Mock; create: jest.Mock } };

  beforeEach(async () => {
    prisma = {
      user: { findUnique: jest.fn(), create: jest.fn() },
    };
    const moduleRef = await Test.createTestingModule({
      providers: [
        AuthService,
        { provide: PrismaService, useValue: prisma },
        { provide: JwtService, useValue: { sign: () => 'signed-token' } },
      ],
    }).compile();
    service = moduleRef.get(AuthService);
  });

  it('register creates user and returns token', async () => {
    prisma.user.findUnique.mockResolvedValue(null);
    prisma.user.create.mockResolvedValue({ id: 'u1', email: 'a@b.c' });

    const result = await service.register('a@b.c', 'password123');

    expect(prisma.user.create).toHaveBeenCalled();
    expect(result).toEqual({ token: 'signed-token' });
  });

  it('register throws on duplicate email', async () => {
    prisma.user.findUnique.mockResolvedValue({ id: 'u1' });
    await expect(service.register('a@b.c', 'password123')).rejects.toThrow(
      ConflictException,
    );
  });
});
```

- [ ] **Step 3: Запустить тест — убедиться, что падает**

Run (из `api/`):
```
npm test -- auth.service
```
Expected: FAIL — `Cannot find module './auth.service'`.

- [ ] **Step 4: Реализовать DTO**

Create: `api/src/auth/dto/auth.dto.ts`:
```typescript
import { IsEmail, MinLength } from 'class-validator';

export class AuthDto {
  @IsEmail()
  email: string;

  @MinLength(8)
  password: string;
}
```

- [ ] **Step 5: Реализовать AuthService**

Create: `api/src/auth/auth.service.ts`:
```typescript
import {
  ConflictException,
  Injectable,
  UnauthorizedException,
} from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import * as bcrypt from 'bcrypt';
import { PrismaService } from '../prisma/prisma.service';

@Injectable()
export class AuthService {
  constructor(
    private prisma: PrismaService,
    private jwt: JwtService,
  ) {}

  async register(email: string, password: string) {
    const existing = await this.prisma.user.findUnique({ where: { email } });
    if (existing) throw new ConflictException('Email already registered');
    const passwordHash = await bcrypt.hash(password, 10);
    const user = await this.prisma.user.create({
      data: { email, passwordHash },
    });
    return this.tokenFor(user.id, user.email);
  }

  async login(email: string, password: string) {
    const user = await this.prisma.user.findUnique({ where: { email } });
    if (!user || !(await bcrypt.compare(password, user.passwordHash))) {
      throw new UnauthorizedException('Invalid credentials');
    }
    return this.tokenFor(user.id, user.email);
  }

  private tokenFor(sub: string, email: string) {
    return { token: this.jwt.sign({ sub, email }) };
  }
}
```

- [ ] **Step 6: Запустить тест — убедиться, что проходит**

Run (из `api/`):
```
npm test -- auth.service
```
Expected: PASS (2 теста).

- [ ] **Step 7: JWT strategy и guard**

Create: `api/src/auth/jwt.strategy.ts`:
```typescript
import { Injectable } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { PassportStrategy } from '@nestjs/passport';
import { ExtractJwt, Strategy } from 'passport-jwt';

@Injectable()
export class JwtStrategy extends PassportStrategy(Strategy) {
  constructor(config: ConfigService) {
    super({
      jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
      secretOrKey: config.get<string>('JWT_SECRET'),
    });
  }

  async validate(payload: { sub: string; email: string }) {
    return { userId: payload.sub, email: payload.email };
  }
}
```

Create: `api/src/auth/jwt-auth.guard.ts`:
```typescript
import { Injectable } from '@nestjs/common';
import { AuthGuard } from '@nestjs/passport';

@Injectable()
export class JwtAuthGuard extends AuthGuard('jwt') {}
```

- [ ] **Step 8: Controller и Module**

Create: `api/src/auth/auth.controller.ts`:
```typescript
import { Body, Controller, Post } from '@nestjs/common';
import { AuthService } from './auth.service';
import { AuthDto } from './dto/auth.dto';

@Controller('auth')
export class AuthController {
  constructor(private auth: AuthService) {}

  @Post('register')
  register(@Body() dto: AuthDto) {
    return this.auth.register(dto.email, dto.password);
  }

  @Post('login')
  login(@Body() dto: AuthDto) {
    return this.auth.login(dto.email, dto.password);
  }
}
```

Create: `api/src/auth/auth.module.ts`:
```typescript
import { Module } from '@nestjs/common';
import { JwtModule } from '@nestjs/jwt';
import { ConfigModule, ConfigService } from '@nestjs/config';
import { PassportModule } from '@nestjs/passport';
import { AuthService } from './auth.service';
import { AuthController } from './auth.controller';
import { JwtStrategy } from './jwt.strategy';

@Module({
  imports: [
    PassportModule,
    JwtModule.registerAsync({
      imports: [ConfigModule],
      inject: [ConfigService],
      useFactory: (config: ConfigService) => ({
        secret: config.get<string>('JWT_SECRET'),
        signOptions: { expiresIn: '1d' },
      }),
    }),
  ],
  controllers: [AuthController],
  providers: [AuthService, JwtStrategy],
})
export class AuthModule {}
```

- [ ] **Step 9: Подключить AuthModule**

Modify: `api/src/app.module.ts` — добавить `AuthModule` в `imports`.

- [ ] **Step 10: Запустить все тесты**

Run (из `api/`):
```
npm test
```
Expected: PASS.

- [ ] **Step 11: Коммит**

```
git add -A
git commit -m "feat: auth with register/login and JWT"
```

---

## Phase 2 — Notes CRUD + очередь

### Task 4: Декоратор текущего пользователя

**Files:**
- Create: `api/src/auth/current-user.decorator.ts`

- [ ] **Step 1: Создать декоратор**

Create: `api/src/auth/current-user.decorator.ts`:
```typescript
import { createParamDecorator, ExecutionContext } from '@nestjs/common';

export const CurrentUser = createParamDecorator(
  (_data: unknown, ctx: ExecutionContext) => {
    const request = ctx.switchToHttp().getRequest();
    return request.user as { userId: string; email: string };
  },
);
```

- [ ] **Step 2: Коммит**

```
git add -A
git commit -m "feat: current-user decorator"
```

---

### Task 5: NotesService (CRUD + постановка в очередь)

**Files:**
- Create: `api/src/notes/notes.service.ts`
- Create: `api/src/notes/dto/note.dto.ts`
- Test: `api/src/notes/notes.service.spec.ts`

- [ ] **Step 1: Написать падающий тест**

Create: `api/src/notes/notes.service.spec.ts`:
```typescript
import { Test } from '@nestjs/testing';
import { NotFoundException } from '@nestjs/common';
import { NotesService } from './notes.service';
import { PrismaService } from '../prisma/prisma.service';
import { SUMMARIZE_QUEUE } from '../jobs/jobs.constants';
import { getQueueToken } from '@nestjs/bullmq';

describe('NotesService', () => {
  let service: NotesService;
  let prisma: any;
  let queue: { add: jest.Mock };

  beforeEach(async () => {
    prisma = {
      note: {
        create: jest.fn(),
        findMany: jest.fn(),
        findFirst: jest.fn(),
        update: jest.fn(),
        delete: jest.fn(),
      },
    };
    queue = { add: jest.fn() };
    const moduleRef = await Test.createTestingModule({
      providers: [
        NotesService,
        { provide: PrismaService, useValue: prisma },
        { provide: getQueueToken(SUMMARIZE_QUEUE), useValue: queue },
      ],
    }).compile();
    service = moduleRef.get(NotesService);
  });

  it('create saves note and enqueues a summarize job', async () => {
    prisma.note.create.mockResolvedValue({ id: 'n1', title: 't', content: 'c' });

    const note = await service.create('u1', { title: 't', content: 'c' });

    expect(prisma.note.create).toHaveBeenCalledWith({
      data: { userId: 'u1', title: 't', content: 'c' },
    });
    expect(queue.add).toHaveBeenCalledWith('summarize', { noteId: 'n1' });
    expect(note.id).toBe('n1');
  });

  it('findOne throws when note not owned', async () => {
    prisma.note.findFirst.mockResolvedValue(null);
    await expect(service.findOne('u1', 'n1')).rejects.toThrow(NotFoundException);
  });
});
```

- [ ] **Step 2: Поставить BullMQ-зависимости**

Run (из `api/`):
```
npm install @nestjs/bullmq bullmq
```

- [ ] **Step 3: Создать константы очереди**

Create: `api/src/jobs/jobs.constants.ts`:
```typescript
export const SUMMARIZE_QUEUE = 'summarize-queue';
```

- [ ] **Step 4: Запустить тест — убедиться, что падает**

Run (из `api/`):
```
npm test -- notes.service
```
Expected: FAIL — `Cannot find module './notes.service'`.

- [ ] **Step 5: Создать DTO**

Create: `api/src/notes/dto/note.dto.ts`:
```typescript
import { IsOptional, IsString, MinLength } from 'class-validator';

export class CreateNoteDto {
  @IsString()
  @MinLength(1)
  title: string;

  @IsString()
  @MinLength(1)
  content: string;
}

export class UpdateNoteDto {
  @IsOptional()
  @IsString()
  @MinLength(1)
  title?: string;

  @IsOptional()
  @IsString()
  @MinLength(1)
  content?: string;
}
```

- [ ] **Step 6: Реализовать NotesService**

Create: `api/src/notes/notes.service.ts`:
```typescript
import { Injectable, NotFoundException } from '@nestjs/common';
import { InjectQueue } from '@nestjs/bullmq';
import { Queue } from 'bullmq';
import { PrismaService } from '../prisma/prisma.service';
import { SUMMARIZE_QUEUE } from '../jobs/jobs.constants';
import { CreateNoteDto, UpdateNoteDto } from './dto/note.dto';

@Injectable()
export class NotesService {
  constructor(
    private prisma: PrismaService,
    @InjectQueue(SUMMARIZE_QUEUE) private queue: Queue,
  ) {}

  async create(userId: string, dto: CreateNoteDto) {
    const note = await this.prisma.note.create({
      data: { userId, title: dto.title, content: dto.content },
    });
    await this.queue.add('summarize', { noteId: note.id });
    return note;
  }

  findAll(userId: string) {
    return this.prisma.note.findMany({
      where: { userId },
      orderBy: { createdAt: 'desc' },
    });
  }

  async findOne(userId: string, id: string) {
    const note = await this.prisma.note.findFirst({ where: { id, userId } });
    if (!note) throw new NotFoundException('Note not found');
    return note;
  }

  async update(userId: string, id: string, dto: UpdateNoteDto) {
    await this.findOne(userId, id);
    const note = await this.prisma.note.update({
      where: { id },
      data: { ...dto, status: 'PENDING', summary: null },
    });
    await this.queue.add('summarize', { noteId: note.id });
    return note;
  }

  async remove(userId: string, id: string) {
    await this.findOne(userId, id);
    await this.prisma.note.delete({ where: { id } });
  }
}
```

- [ ] **Step 7: Запустить тест — убедиться, что проходит**

Run (из `api/`):
```
npm test -- notes.service
```
Expected: PASS (2 теста).

- [ ] **Step 8: Коммит**

```
git add -A
git commit -m "feat: NotesService with CRUD and queue enqueue"
```

---

### Task 6: NotesController + NotesModule

**Files:**
- Create: `api/src/notes/notes.controller.ts`
- Create: `api/src/notes/notes.module.ts`

- [ ] **Step 1: Создать контроллер**

Create: `api/src/notes/notes.controller.ts`:
```typescript
import {
  Body,
  Controller,
  Delete,
  Get,
  HttpCode,
  Param,
  Patch,
  Post,
  UseGuards,
} from '@nestjs/common';
import { JwtAuthGuard } from '../auth/jwt-auth.guard';
import { CurrentUser } from '../auth/current-user.decorator';
import { NotesService } from './notes.service';
import { CreateNoteDto, UpdateNoteDto } from './dto/note.dto';

@UseGuards(JwtAuthGuard)
@Controller('notes')
export class NotesController {
  constructor(private notes: NotesService) {}

  @Get()
  findAll(@CurrentUser() user: { userId: string }) {
    return this.notes.findAll(user.userId);
  }

  @Post()
  create(
    @CurrentUser() user: { userId: string },
    @Body() dto: CreateNoteDto,
  ) {
    return this.notes.create(user.userId, dto);
  }

  @Get(':id')
  findOne(@CurrentUser() user: { userId: string }, @Param('id') id: string) {
    return this.notes.findOne(user.userId, id);
  }

  @Patch(':id')
  update(
    @CurrentUser() user: { userId: string },
    @Param('id') id: string,
    @Body() dto: UpdateNoteDto,
  ) {
    return this.notes.update(user.userId, id, dto);
  }

  @Delete(':id')
  @HttpCode(204)
  remove(@CurrentUser() user: { userId: string }, @Param('id') id: string) {
    return this.notes.remove(user.userId, id);
  }
}
```

- [ ] **Step 2: Создать модуль**

Create: `api/src/notes/notes.module.ts`:
```typescript
import { Module } from '@nestjs/common';
import { BullModule } from '@nestjs/bullmq';
import { NotesService } from './notes.service';
import { NotesController } from './notes.controller';
import { SUMMARIZE_QUEUE } from '../jobs/jobs.constants';

@Module({
  imports: [BullModule.registerQueue({ name: SUMMARIZE_QUEUE })],
  controllers: [NotesController],
  providers: [NotesService],
})
export class NotesModule {}
```

- [ ] **Step 3: Подключить BullMQ глобально + NotesModule**

Modify: `api/src/app.module.ts`:
```typescript
import { Module } from '@nestjs/common';
import { ConfigModule, ConfigService } from '@nestjs/config';
import { BullModule } from '@nestjs/bullmq';
import { PrismaModule } from './prisma/prisma.module';
import { AuthModule } from './auth/auth.module';
import { NotesModule } from './notes/notes.module';

@Module({
  imports: [
    ConfigModule.forRoot({ isGlobal: true }),
    BullModule.forRootAsync({
      inject: [ConfigService],
      useFactory: (config: ConfigService) => ({
        connection: {
          host: config.get('REDIS_HOST', 'localhost'),
          port: Number(config.get('REDIS_PORT', 6379)),
        },
      }),
    }),
    PrismaModule,
    AuthModule,
    NotesModule,
  ],
})
export class AppModule {}
```

- [ ] **Step 4: Запустить тесты**

Run (из `api/`):
```
npm test
```
Expected: PASS.

- [ ] **Step 5: Коммит**

```
git add -A
git commit -m "feat: notes controller and module"
```

---

## Phase 3 — AI-интеграция и воркер

### Task 7: SummariesService (Claude + мок)

**Files:**
- Create: `api/src/summaries/summaries.service.ts`
- Create: `api/src/summaries/summaries.module.ts`
- Test: `api/src/summaries/summaries.service.spec.ts`

- [ ] **Step 1: Поставить Anthropic SDK**

Run (из `api/`):
```
npm install @anthropic-ai/sdk
```

- [ ] **Step 2: Написать падающий тест (мок-режим без ключа)**

Create: `api/src/summaries/summaries.service.spec.ts`:
```typescript
import { Test } from '@nestjs/testing';
import { ConfigService } from '@nestjs/config';
import { SummariesService } from './summaries.service';

describe('SummariesService', () => {
  async function build(apiKey: string) {
    const moduleRef = await Test.createTestingModule({
      providers: [
        SummariesService,
        {
          provide: ConfigService,
          useValue: { get: () => apiKey },
        },
      ],
    }).compile();
    return moduleRef.get(SummariesService);
  }

  it('returns a mock summary when no API key is set', async () => {
    const service = await build('');
    const result = await service.summarize('Title', 'Some long content here');

    expect(result.summary).toContain('[mock]');
    expect(Array.isArray(result.tags)).toBe(true);
    expect(result.tags.length).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 3: Запустить тест — убедиться, что падает**

Run (из `api/`):
```
npm test -- summaries.service
```
Expected: FAIL — модуль не найден.

- [ ] **Step 4: Реализовать SummariesService**

Create: `api/src/summaries/summaries.service.ts`:
```typescript
import { Injectable, Logger } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import Anthropic from '@anthropic-ai/sdk';

export interface SummaryResult {
  summary: string;
  tags: string[];
}

@Injectable()
export class SummariesService {
  private readonly logger = new Logger(SummariesService.name);
  private readonly client: Anthropic | null;

  constructor(private config: ConfigService) {
    const key = this.config.get<string>('ANTHROPIC_API_KEY');
    this.client = key ? new Anthropic({ apiKey: key }) : null;
  }

  async summarize(title: string, content: string): Promise<SummaryResult> {
    if (!this.client) {
      return this.mock(title, content);
    }
    const response = await this.client.messages.create({
      model: 'claude-haiku-4-5-20251001',
      max_tokens: 300,
      messages: [
        {
          role: 'user',
          content:
            `Summarize the following note in 1-2 sentences and propose 3 short topic tags. ` +
            `Respond as JSON: {"summary": string, "tags": string[]}.\n\n` +
            `Title: ${title}\n\nContent:\n${content}`,
        },
      ],
    });
    const text = response.content
      .filter((b): b is { type: 'text'; text: string } => b.type === 'text')
      .map((b) => b.text)
      .join('');
    try {
      const parsed = JSON.parse(text) as SummaryResult;
      return { summary: parsed.summary, tags: parsed.tags ?? [] };
    } catch {
      this.logger.warn('Failed to parse Claude JSON, using raw text');
      return { summary: text.slice(0, 280), tags: [] };
    }
  }

  private mock(title: string, content: string): SummaryResult {
    const firstWords = content.split(/\s+/).slice(0, 12).join(' ');
    return {
      summary: `[mock] ${title}: ${firstWords}...`,
      tags: ['mock', 'note'],
    };
  }
}
```

- [ ] **Step 5: Запустить тест — убедиться, что проходит**

Run (из `api/`):
```
npm test -- summaries.service
```
Expected: PASS.

- [ ] **Step 6: Создать SummariesModule**

Create: `api/src/summaries/summaries.module.ts`:
```typescript
import { Module } from '@nestjs/common';
import { SummariesService } from './summaries.service';

@Module({
  providers: [SummariesService],
  exports: [SummariesService],
})
export class SummariesModule {}
```

- [ ] **Step 7: Коммит**

```
git add -A
git commit -m "feat: SummariesService with Claude integration and mock fallback"
```

---

### Task 8: BullMQ-процессор (воркер)

**Files:**
- Create: `api/src/jobs/summarize.processor.ts`
- Create: `api/src/jobs/jobs.module.ts`
- Test: `api/src/jobs/summarize.processor.spec.ts`

- [ ] **Step 1: Написать падающий тест процессора**

Create: `api/src/jobs/summarize.processor.spec.ts`:
```typescript
import { Test } from '@nestjs/testing';
import { SummarizeProcessor } from './summarize.processor';
import { PrismaService } from '../prisma/prisma.service';
import { SummariesService } from '../summaries/summaries.service';

describe('SummarizeProcessor', () => {
  let processor: SummarizeProcessor;
  let prisma: any;
  let summaries: { summarize: jest.Mock };

  beforeEach(async () => {
    prisma = {
      note: {
        findUnique: jest.fn(),
        update: jest.fn(),
      },
    };
    summaries = { summarize: jest.fn() };
    const moduleRef = await Test.createTestingModule({
      providers: [
        SummarizeProcessor,
        { provide: PrismaService, useValue: prisma },
        { provide: SummariesService, useValue: summaries },
      ],
    }).compile();
    processor = moduleRef.get(SummarizeProcessor);
  });

  it('summarizes the note and marks it DONE', async () => {
    prisma.note.findUnique.mockResolvedValue({
      id: 'n1',
      title: 't',
      content: 'c',
    });
    summaries.summarize.mockResolvedValue({ summary: 's', tags: ['a'] });

    await processor.process({ data: { noteId: 'n1' } } as any);

    expect(prisma.note.update).toHaveBeenCalledWith({
      where: { id: 'n1' },
      data: { summary: 's', tags: ['a'], status: 'DONE' },
    });
  });
});
```

- [ ] **Step 2: Запустить тест — убедиться, что падает**

Run (из `api/`):
```
npm test -- summarize.processor
```
Expected: FAIL — модуль не найден.

- [ ] **Step 3: Реализовать процессор**

Create: `api/src/jobs/summarize.processor.ts`:
```typescript
import { Processor, WorkerHost } from '@nestjs/bullmq';
import { Logger } from '@nestjs/common';
import { Job } from 'bullmq';
import { PrismaService } from '../prisma/prisma.service';
import { SummariesService } from '../summaries/summaries.service';
import { SUMMARIZE_QUEUE } from './jobs.constants';

@Processor(SUMMARIZE_QUEUE)
export class SummarizeProcessor extends WorkerHost {
  private readonly logger = new Logger(SummarizeProcessor.name);

  constructor(
    private prisma: PrismaService,
    private summaries: SummariesService,
  ) {
    super();
  }

  async process(job: Job<{ noteId: string }>): Promise<void> {
    const { noteId } = job.data;
    const note = await this.prisma.note.findUnique({ where: { id: noteId } });
    if (!note) {
      this.logger.warn(`Note ${noteId} not found, skipping`);
      return;
    }
    await this.prisma.note.update({
      where: { id: noteId },
      data: { status: 'PROCESSING' },
    });
    const result = await this.summaries.summarize(note.title, note.content);
    await this.prisma.note.update({
      where: { id: noteId },
      data: { summary: result.summary, tags: result.tags, status: 'DONE' },
    });
  }

  async onFailed(job: Job<{ noteId: string }>) {
    await this.prisma.note.update({
      where: { id: job.data.noteId },
      data: { status: 'FAILED' },
    });
  }
}
```

- [ ] **Step 4: Запустить тест — убедиться, что проходит**

Run (из `api/`):
```
npm test -- summarize.processor
```
Expected: PASS.

- [ ] **Step 5: Создать JobsModule**

Create: `api/src/jobs/jobs.module.ts`:
```typescript
import { Module } from '@nestjs/common';
import { BullModule } from '@nestjs/bullmq';
import { SummarizeProcessor } from './summarize.processor';
import { SummariesModule } from '../summaries/summaries.module';
import { SUMMARIZE_QUEUE } from './jobs.constants';

@Module({
  imports: [
    BullModule.registerQueue({
      name: SUMMARIZE_QUEUE,
      defaultJobOptions: {
        attempts: 3,
        backoff: { type: 'exponential', delay: 2000 },
      },
    }),
    SummariesModule,
  ],
  providers: [SummarizeProcessor],
})
export class JobsModule {}
```

- [ ] **Step 6: Подключить JobsModule**

Modify: `api/src/app.module.ts` — добавить `JobsModule` в `imports`.

- [ ] **Step 7: Запустить все тесты**

Run (из `api/`):
```
npm test
```
Expected: PASS.

- [ ] **Step 8: Коммит**

```
git add -A
git commit -m "feat: BullMQ summarize processor (background worker)"
```

---

### Task 9: Health endpoint

**Files:**
- Create: `api/src/health/health.controller.ts`
- Create: `api/src/health/health.module.ts`

- [ ] **Step 1: Контроллер**

Create: `api/src/health/health.controller.ts`:
```typescript
import { Controller, Get } from '@nestjs/common';

@Controller('health')
export class HealthController {
  @Get()
  check() {
    return { status: 'ok' };
  }
}
```

- [ ] **Step 2: Модуль и подключение**

Create: `api/src/health/health.module.ts`:
```typescript
import { Module } from '@nestjs/common';
import { HealthController } from './health.controller';

@Module({ controllers: [HealthController] })
export class HealthModule {}
```
Modify: `api/src/app.module.ts` — добавить `HealthModule`.

- [ ] **Step 3: Коммит**

```
git add -A
git commit -m "feat: health endpoint"
```

---

### Task 10: e2e-тест основного сценария

**Files:**
- Create: `api/test/notes.e2e-spec.ts`
- Modify: `api/test/jest-e2e.json` (если нужно)

- [ ] **Step 1: Написать e2e-тест**

Create: `api/test/notes.e2e-spec.ts`:
```typescript
import { Test } from '@nestjs/testing';
import { INestApplication, ValidationPipe } from '@nestjs/common';
import * as request from 'supertest';
import { AppModule } from '../src/app.module';

describe('Notes flow (e2e)', () => {
  let app: INestApplication;
  let token: string;
  const email = `e2e-${Date.now()}@test.local`;

  beforeAll(async () => {
    const moduleRef = await Test.createTestingModule({
      imports: [AppModule],
    }).compile();
    app = moduleRef.createNestApplication();
    app.useGlobalPipes(new ValidationPipe({ whitelist: true, transform: true }));
    await app.init();
  });

  afterAll(async () => {
    await app.close();
  });

  it('register → login → create note → get note', async () => {
    await request(app.getHttpServer())
      .post('/auth/register')
      .send({ email, password: 'password123' })
      .expect(201);

    const login = await request(app.getHttpServer())
      .post('/auth/login')
      .send({ email, password: 'password123' })
      .expect(201);
    token = login.body.token;
    expect(token).toBeDefined();

    const created = await request(app.getHttpServer())
      .post('/notes')
      .set('Authorization', `Bearer ${token}`)
      .send({ title: 'Hello', content: 'This is a test note body.' })
      .expect(201);
    expect(created.body.status).toBe('PENDING');

    await request(app.getHttpServer())
      .get(`/notes/${created.body.id}`)
      .set('Authorization', `Bearer ${token}`)
      .expect(200);
  });
});
```

- [ ] **Step 2: Запустить e2e (нужны запущенные Postgres и Redis)**

Run (из `api/`):
```
docker run --name ainotes-redis -p 6379:6379 -d redis:7
npm run test:e2e
```
Expected: PASS. (Postgres уже поднят из Task 2; если нет — подними контейнер `ainotes-pg`.)

- [ ] **Step 3: Коммит**

```
git add -A
git commit -m "test: e2e for register/login/notes flow"
```

---

## Phase 4 — Frontend (Angular)

### Task 11: Каркас Angular + конфиг API

**Files:**
- Create: `web/` (через CLI)
- Create: `web/src/environments/environment.ts`

- [ ] **Step 1: Создать Angular-проект**

Run (из `test/`):
```
npx -y @angular/cli@latest new web --routing --style=css --ssr=false --skip-git
```
Expected: создаётся `web/`.

- [ ] **Step 2: Прописать базовый URL API**

Create: `web/src/environments/environment.ts`:
```typescript
export const environment = {
  apiUrl: 'http://localhost:3000',
};
```

- [ ] **Step 3: Проверить запуск**

Run (из `web/`):
```
npm start
```
Expected: приложение на `http://localhost:4200`. Останови.

- [ ] **Step 4: Коммит**

```
git add -A
git commit -m "chore: scaffold Angular web app"
```

---

### Task 12: AuthService + JWT-интерсептор

**Files:**
- Create: `web/src/app/core/auth.service.ts`
- Create: `web/src/app/core/auth.interceptor.ts`
- Test: `web/src/app/core/auth.service.spec.ts`
- Modify: `web/src/app/app.config.ts`

- [ ] **Step 1: Написать падающий тест AuthService**

Create: `web/src/app/core/auth.service.spec.ts`:
```typescript
import { TestBed } from '@angular/core/testing';
import {
  HttpTestingController,
  provideHttpClientTesting,
} from '@angular/common/http/testing';
import { provideHttpClient } from '@angular/common/http';
import { AuthService } from './auth.service';
import { environment } from '../../environments/environment';

describe('AuthService', () => {
  let service: AuthService;
  let http: HttpTestingController;

  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [
        AuthService,
        provideHttpClient(),
        provideHttpClientTesting(),
      ],
    });
    service = TestBed.inject(AuthService);
    http = TestBed.inject(HttpTestingController);
    localStorage.clear();
  });

  it('login stores the token', () => {
    service.login('a@b.c', 'password123').subscribe();
    const req = http.expectOne(`${environment.apiUrl}/auth/login`);
    req.flush({ token: 'abc' });
    expect(service.token()).toBe('abc');
  });
});
```

- [ ] **Step 2: Запустить тест — убедиться, что падает**

Run (из `web/`):
```
npm test -- --watch=false
```
Expected: FAIL — нет `auth.service`.

- [ ] **Step 3: Реализовать AuthService**

Create: `web/src/app/core/auth.service.ts`:
```typescript
import { Injectable, signal } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { tap } from 'rxjs';
import { environment } from '../../environments/environment';

@Injectable({ providedIn: 'root' })
export class AuthService {
  private readonly _token = signal<string | null>(
    localStorage.getItem('token'),
  );
  readonly token = this._token.asReadonly();

  constructor(private http: HttpClient) {}

  register(email: string, password: string) {
    return this.http
      .post<{ token: string }>(`${environment.apiUrl}/auth/register`, {
        email,
        password,
      })
      .pipe(tap((res) => this.setToken(res.token)));
  }

  login(email: string, password: string) {
    return this.http
      .post<{ token: string }>(`${environment.apiUrl}/auth/login`, {
        email,
        password,
      })
      .pipe(tap((res) => this.setToken(res.token)));
  }

  logout() {
    localStorage.removeItem('token');
    this._token.set(null);
  }

  private setToken(token: string) {
    localStorage.setItem('token', token);
    this._token.set(token);
  }
}
```

- [ ] **Step 4: Запустить тест — убедиться, что проходит**

Run (из `web/`):
```
npm test -- --watch=false
```
Expected: PASS.

- [ ] **Step 5: Создать интерсептор**

Create: `web/src/app/core/auth.interceptor.ts`:
```typescript
import { HttpInterceptorFn } from '@angular/common/http';
import { inject } from '@angular/core';
import { AuthService } from './auth.service';

export const authInterceptor: HttpInterceptorFn = (req, next) => {
  const token = inject(AuthService).token();
  if (token) {
    req = req.clone({
      setHeaders: { Authorization: `Bearer ${token}` },
    });
  }
  return next(req);
};
```

- [ ] **Step 6: Зарегистрировать HttpClient + интерсептор**

Modify: `web/src/app/app.config.ts`:
```typescript
import { ApplicationConfig } from '@angular/core';
import { provideRouter } from '@angular/router';
import { provideHttpClient, withInterceptors } from '@angular/common/http';
import { routes } from './app.routes';
import { authInterceptor } from './core/auth.interceptor';

export const appConfig: ApplicationConfig = {
  providers: [
    provideRouter(routes),
    provideHttpClient(withInterceptors([authInterceptor])),
  ],
};
```

- [ ] **Step 7: Коммит**

```
git add -A
git commit -m "feat: web auth service and jwt interceptor"
```

---

### Task 13: NotesService (web) + страницы

**Files:**
- Create: `web/src/app/core/notes.service.ts`
- Create: `web/src/app/notes/notes-list.component.ts`
- Create: `web/src/app/auth/login.component.ts`
- Modify: `web/src/app/app.routes.ts`

- [ ] **Step 1: Создать модель и NotesService**

Create: `web/src/app/core/notes.service.ts`:
```typescript
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { environment } from '../../environments/environment';

export interface Note {
  id: string;
  title: string;
  content: string;
  summary: string | null;
  tags: string[];
  status: 'PENDING' | 'PROCESSING' | 'DONE' | 'FAILED';
}

@Injectable({ providedIn: 'root' })
export class NotesService {
  private base = `${environment.apiUrl}/notes`;
  constructor(private http: HttpClient) {}

  list() {
    return this.http.get<Note[]>(this.base);
  }

  create(title: string, content: string) {
    return this.http.post<Note>(this.base, { title, content });
  }

  get(id: string) {
    return this.http.get<Note>(`${this.base}/${id}`);
  }
}
```

- [ ] **Step 2: Login-компонент**

Create: `web/src/app/auth/login.component.ts`:
```typescript
import { Component, inject } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { Router } from '@angular/router';
import { AuthService } from '../core/auth.service';

@Component({
  selector: 'app-login',
  standalone: true,
  imports: [FormsModule],
  template: `
    <h2>Login</h2>
    <form (ngSubmit)="submit()">
      <input [(ngModel)]="email" name="email" placeholder="email" />
      <input
        [(ngModel)]="password"
        name="password"
        type="password"
        placeholder="password"
      />
      <button type="submit">Sign in</button>
      <button type="button" (click)="register()">Register</button>
    </form>
    @if (error) {
      <p style="color:red">{{ error }}</p>
    }
  `,
})
export class LoginComponent {
  private auth = inject(AuthService);
  private router = inject(Router);
  email = '';
  password = '';
  error = '';

  submit() {
    this.auth.login(this.email, this.password).subscribe({
      next: () => this.router.navigateByUrl('/notes'),
      error: () => (this.error = 'Login failed'),
    });
  }

  register() {
    this.auth.register(this.email, this.password).subscribe({
      next: () => this.router.navigateByUrl('/notes'),
      error: () => (this.error = 'Register failed'),
    });
  }
}
```

- [ ] **Step 3: Notes-list компонент**

Create: `web/src/app/notes/notes-list.component.ts`:
```typescript
import { Component, inject, signal, OnInit } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { NotesService, Note } from '../core/notes.service';

@Component({
  selector: 'app-notes-list',
  standalone: true,
  imports: [FormsModule],
  template: `
    <h2>My notes</h2>
    <form (ngSubmit)="add()">
      <input [(ngModel)]="title" name="title" placeholder="title" />
      <textarea [(ngModel)]="content" name="content" placeholder="content"></textarea>
      <button type="submit">Add</button>
    </form>
    <button (click)="reload()">Refresh</button>
    <ul>
      @for (note of notes(); track note.id) {
        <li>
          <strong>{{ note.title }}</strong> — {{ note.status }}
          @if (note.summary) {
            <p><em>{{ note.summary }}</em></p>
            <small>{{ note.tags.join(', ') }}</small>
          }
        </li>
      }
    </ul>
  `,
})
export class NotesListComponent implements OnInit {
  private service = inject(NotesService);
  notes = signal<Note[]>([]);
  title = '';
  content = '';

  ngOnInit() {
    this.reload();
  }

  reload() {
    this.service.list().subscribe((n) => this.notes.set(n));
  }

  add() {
    this.service.create(this.title, this.content).subscribe(() => {
      this.title = '';
      this.content = '';
      this.reload();
    });
  }
}
```

- [ ] **Step 4: Роуты**

Modify: `web/src/app/app.routes.ts`:
```typescript
import { Routes } from '@angular/router';
import { LoginComponent } from './auth/login.component';
import { NotesListComponent } from './notes/notes-list.component';

export const routes: Routes = [
  { path: '', redirectTo: 'login', pathMatch: 'full' },
  { path: 'login', component: LoginComponent },
  { path: 'notes', component: NotesListComponent },
];
```

- [ ] **Step 5: Убедиться, что app.component рендерит router-outlet**

Modify: `web/src/app/app.component.ts` (или `app.ts` в новых версиях CLI) — шаблон должен содержать `<router-outlet />` и импортировать `RouterOutlet`. Если CLI уже сгенерировал это — оставь.

- [ ] **Step 6: Ручная проверка**

Run: запусти `api` (npm run start), `web` (npm start), Postgres и Redis. Открой `http://localhost:4200`, зарегистрируйся, добавь заметку, нажми Refresh — увидишь статус и (через секунду) mock-summary.

- [ ] **Step 7: Коммит**

```
git add -A
git commit -m "feat: web notes list, login page and routes"
```

---

## Phase 5 — Инфраструктура и документация

### Task 14: Dockerfile для API

**Files:**
- Create: `api/Dockerfile`
- Create: `api/.dockerignore`

- [ ] **Step 1: Dockerfile**

Create: `api/Dockerfile`:
```dockerfile
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npx prisma generate && npm run build
EXPOSE 3000
CMD ["sh", "-c", "npx prisma migrate deploy && node dist/main.js"]
```

Create: `api/.dockerignore`:
```
node_modules
dist
.env
```

- [ ] **Step 2: Коммит**

```
git add -A
git commit -m "chore: api Dockerfile"
```

---

### Task 15: docker-compose

**Files:**
- Create: `docker-compose.yml`

- [ ] **Step 1: Compose-файл**

Create: `docker-compose.yml`:
```yaml
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ainotes
    ports:
      - '5432:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 5s
      timeout: 3s
      retries: 5

  redis:
    image: redis:7
    ports:
      - '6379:6379'

  api:
    build: ./api
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/ainotes?schema=public
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: dev-secret-change-me
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
    ports:
      - '3000:3000'
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
```

- [ ] **Step 2: Проверить запуск стека**

Run (из `test/`, останови локальные контейнеры `ainotes-pg`/`ainotes-redis`, чтобы не конфликтовали порты):
```
docker compose up --build
```
Expected: `api` стартует после готовности Postgres, миграции применяются, `GET http://localhost:3000/health` → `{"status":"ok"}`.

- [ ] **Step 3: Коммит**

```
git add -A
git commit -m "chore: docker-compose for full stack"
```

---

### Task 16: CI (GitHub Actions)

**Files:**
- Create: `.github/workflows/ci.yml`

- [ ] **Step 1: Workflow**

Create: `.github/workflows/ci.yml`:
```yaml
name: CI

on:
  push:
  pull_request:

jobs:
  api:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: ainotes
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s --health-timeout 3s --health-retries 5
      redis:
        image: redis:7
        ports: ['6379:6379']
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/ainotes?schema=public
      REDIS_HOST: localhost
      REDIS_PORT: 6379
      JWT_SECRET: ci-secret
      ANTHROPIC_API_KEY: ''
    defaults:
      run:
        working-directory: api
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npx prisma generate
      - run: npx prisma migrate deploy
      - run: npm run lint
      - run: npm test
      - run: npm run test:e2e
      - run: npm run build

  web:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run build
      - run: npm test -- --watch=false --browsers=ChromeHeadless
```

- [ ] **Step 2: Коммит**

```
git add -A
git commit -m "ci: GitHub Actions for api and web"
```

---

### Task 17: README и RUNBOOK

**Files:**
- Create: `README.md`
- Create: `RUNBOOK.md`

- [ ] **Step 1: README**

Create: `README.md`:
```markdown
# AI Notes

Full-stack учебный проект: заметки с AI-summary (Angular + NestJS + Postgres + Redis/BullMQ + Claude).

## Быстрый старт (Docker)

1. (Опционально) экспортируй ключ: `export ANTHROPIC_API_KEY=sk-...` (без него работает мок).
2. `docker compose up --build`
3. API: http://localhost:3000/health , фронт запускается отдельно (см. ниже).

## Локальная разработка

```
# инфраструктура
docker compose up postgres redis -d
# backend
cd api && npm install && npx prisma migrate dev && npm run start:dev
# worker встроен в тот же процесс (BullMQ processor)
# frontend
cd web && npm install && npm start
```

Открой http://localhost:4200 , зарегистрируйся, добавь заметку.

## Переменные окружения (api)

| Переменная | Назначение |
|---|---|
| DATABASE_URL | строка подключения к Postgres |
| REDIS_HOST / REDIS_PORT | подключение к Redis |
| JWT_SECRET | секрет для подписи JWT |
| ANTHROPIC_API_KEY | ключ Claude (пусто → мок-summary) |

## Тесты

```
cd api && npm test && npm run test:e2e
cd web && npm test -- --watch=false
```
```

- [ ] **Step 2: RUNBOOK**

Create: `RUNBOOK.md`:
```markdown
# RUNBOOK — AI Notes

## Деплой

Образ API собирается из `api/Dockerfile`. На любом хосте с Docker:

```
export ANTHROPIC_API_KEY=sk-...
docker compose up --build -d
```

Миграции применяются автоматически при старте контейнера (`prisma migrate deploy`).

## Rollback

- Образы тегируются по git-SHA в CI (расширение): откат = `docker compose up` с предыдущим тегом образа.
- Откат миграции БД: `npx prisma migrate resolve --rolled-back <migration_name>` + восстановление из бэкапа Postgres. Бэкап: `pg_dump`.
- Быстрый откат кода: `git revert <sha>` → push → CI пересоберёт.

## Логи и наблюдаемость

- Приложение пишет структурные логи (pino) в stdout — `docker compose logs -f api`.
- Точки роста:
  - **Метрики:** добавить `@willsoto/nestjs-prometheus`, отдать `/metrics`, собрать Prometheus.
  - **Трейсинг:** OpenTelemetry SDK + экспортёр.
  - **LLM-observability:** Langfuse вокруг вызовов `SummariesService`.

## Типовые проблемы

| Симптом | Причина | Действие |
|---|---|---|
| Заметки висят в PENDING | воркер/Redis недоступен | проверь `docker compose logs redis api` |
| Все summary `[mock]` | не задан ANTHROPIC_API_KEY | задай ключ и перезапусти api |
| 401 на /notes | истёк/нет JWT | перелогинься |
| api не стартует | Postgres не готов | проверь healthcheck, перезапусти |
```

- [ ] **Step 3: Добавить pino-логирование (закрывает observability-зачаток)**

Run (из `api/`):
```
npm install nestjs-pino pino-http
```
Modify: `api/src/app.module.ts` — добавить `LoggerModule.forRoot()` из `nestjs-pino` в imports.
Modify: `api/src/main.ts` — `app.useLogger(app.get(Logger))` (импорт `Logger` из `nestjs-pino`).

- [ ] **Step 4: Финальный прогон тестов**

Run (из `api/`):
```
npm test
```
Expected: PASS.

- [ ] **Step 5: Коммит**

```
git add -A
git commit -m "docs: README and RUNBOOK; structured logging"
```

---

## Соответствие спеку (self-review карта)

| Раздел спека | Задачи плана |
|---|---|
| Архитектура / поток данных | Task 5, 8, 15 |
| Auth (JWT, bcrypt) | Task 3, 4 |
| Модели + миграции (Prisma) | Task 2 |
| Notes CRUD + API | Task 5, 6 |
| AI-интеграция (Claude + мок) | Task 7 |
| Фоновые задачи (BullMQ) | Task 5 (enqueue), 8 (worker) |
| Health | Task 9 |
| Тесты (unit + e2e) | Task 3,5,7,8,10,12 |
| Frontend (Angular) | Task 11, 12, 13 |
| Docker / Compose | Task 14, 15 |
| CI/CD | Task 16 |
| README / RUNBOOK / observability | Task 17 |

---

## Замечания по обучению

- Делай задачи строго по порядку — каждая опирается на предыдущую.
- После каждого `Commit`-шага останавливайся и убедись, что понимаешь, что произошло.
- Если хочешь увидеть реальный Claude — задай `ANTHROPIC_API_KEY` и сравни с мок-режимом.
- Точки расширения под «плюсы» вакансии: vector DB / RAG (поиск по заметкам), Langfuse (observability LLM), реальный деплой (Render/Railway).
